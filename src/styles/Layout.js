// import styled component
import styled from "styled-components";

const MainLayout = styled.div`
    padding: 1rem 1.5rem;
`;

const InnerLayout = styled.div`
    padding: 2rem 0;
`;

export { InnerLayout, MainLayout };